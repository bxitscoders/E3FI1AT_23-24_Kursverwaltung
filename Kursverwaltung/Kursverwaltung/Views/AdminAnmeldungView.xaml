<Window x:Class="Kursverwaltung.Views.AdminAnmeldungView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="220" Width="320" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="E-Mail-Adresse:" Margin="10,30,10,5"/>
        <TextBox Grid.Row="1" x:Name="AdminMailTextBox" Margin="5"/>

        <TextBlock Grid.Row="2" Text="Passwort:" Margin="10,10,10,5"/>
        <PasswordBox Grid.Row="3" x:Name="PasswordBox" Margin="5" PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Grid.Row="4" Content="Anmelden" Width="100" Margin="20"/>
    </Grid>
</Window>
