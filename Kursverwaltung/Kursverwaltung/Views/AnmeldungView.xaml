<Window x:Class="Kursverwaltung.AnmeldungView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:kursverwaltung="clr-namespace:Kursverwaltung" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        Title="Anmeldung" Height="300" Width="400">
    <Window.DataContext>
        <kursverwaltung:AnmeldungVM />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,100,20,0">

            <Button Command="{Binding UserCommand}" Padding="10" Width="120" >
                <TextBlock Text="User" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>

        <StackPanel Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,100,0,0">
            <Button Command="{Binding AdminCommand}" Padding="10" Width="120">
                <TextBlock Text="Admin" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>
        <Image HorizontalAlignment="Left" Height="100" Margin="70,57,0,0" VerticalAlignment="Top" Width="100" Source="/Views/user.png"/>
        <Image HorizontalAlignment="Left" Height="100" Margin="30,57,0,0" VerticalAlignment="Top" Width="100" Source="/Views/admin.png" Grid.Column="1"/>
    </Grid>
</Window>
